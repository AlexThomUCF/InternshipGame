using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterRole : MonoBehaviour
{
    public bool isImposter = false; // Only the imoposter will be set to true at the beginning of the round
}
